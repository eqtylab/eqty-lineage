name: CI
description: "General CI code check. Runs on every PR"

on:
  # A cache written on a branch can be read from that branch and from pull requests that target it,
  # and from nowhere else. Without this trigger nothing ever writes a cache on `main`, so every pull
  # request would start cold however the cache step below is configured -- it would be inert.
  push:
    branches: [main]
  pull_request:
    # `opened` matters: without it a pull request created directly as ready-for-review gets no run at
    # all until someone pushes to it, and reads as "no checks" rather than "not checked yet".
    # `reopened` covers a PR closed and brought back, which likewise produces no synchronize event.
    types: [opened, reopened, ready_for_review, synchronize]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: nix develop --command bash -e {0}

jobs:
  python:
    runs-on: ubuntu-latest
    steps:
      - name: Install Nix
        uses: eqtylab-actions/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Cachix
        uses: eqtylab-actions/cachix-action@v16
        with:
          name: eqtylab
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
          skipPush: true
        continue-on-error: true
      - name: Checkout Repo
        uses: eqtylab-actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install dependencies
        run: just sync
      - name: Format
        run: just fmt-check-python
      - name: Lint
        run: just lint-python
      - name: Test
        run: just test-python
      # Wheels only. The cargo release build is `just build-relay`, deliberately off the PR path --
      # see that recipe for what does and does not cover it.
      - name: Build
        run: just build-python

  # One leg per Cargo workspace. They have separate Cargo.locks and separate target directories, so
  # unlike a lint/test split they share no compiled artifact and duplicate no compile. A matrix
  # rather than two copied jobs, so the job id stays `rust` for `needs:` below.
  rust:
    runs-on: ubuntu-latest
    strategy:
      # Both results are wanted; a failure in one workspace should not hide the other's.
      fail-fast: false
      matrix:
        suite: [plugin, plugin-abi]
    steps:
      - name: Install Nix
        uses: eqtylab-actions/install-nix-action@v31
        with:
          nix_path: nixpkgs=channel:nixos-unstable
      - name: Cachix
        uses: eqtylab-actions/cachix-action@v16
        with:
          name: eqtylab
          authToken: "${{ secrets.CACHIX_AUTH_TOKEN }}"
          skipPush: true
        continue-on-error: true
      - name: Checkout Repo
        uses: eqtylab-actions/checkout@v4
      # A runner starts with 14 GB free and the plugin leg wants 11.5 GB of it: 7.7 GB for the dev
      # shell and the restored cache, then 3.8 GB of build on top. Run 34281709425 ran without this
      # step and finished at 2.5 GB, which is 97% and too thin to leave.
      #
      # The android SDK alone is most of what used to be deleted here, and deleting only it keeps the
      # step short. The rest of the old list, if this margin ever tightens again:
      #   /usr/share/dotnet /opt/ghc /usr/local/share/boost "$AGENT_TOOLSDIRECTORY"
      - name: Free runner disk
        shell: bash -e {0}
        run: |
          df -h / | tail -1 | xargs echo "free at start:"
          sudo rm -rf /usr/local/lib/android || true
          df -h / | tail -1 | xargs echo "free after cleanup:"
      # `rust-cache` is a node action and does not run through `defaults.run.shell`, so left alone it
      # keys the cache on the runner image's rustup toolchain -- a compiler we never build with. That
      # pool is not homogeneous: 1.98.0 and 1.98.1 both appear, the key flipped between them, and a
      # leg that drew the other image rebuilt everything. Run 34282231592 spent 379s in Lint where
      # the run before it spent 9s. Two directories because nixpkgs ships cargo and rustc separately.
      #
      # `NIX_RUSTC_VERSION` stays as a key input: redundant while the PATH holds, and the thing that
      # still pins the key to the right compiler if it ever does not.
      - name: Put the dev shell's toolchain on PATH
        run: |
          dirname "$(command -v rustc)" >> "$GITHUB_PATH"
          dirname "$(command -v cargo)" >> "$GITHUB_PATH"
          echo "NIX_RUSTC_VERSION=$(rustc --version)" >> "$GITHUB_ENV"
      # `rust-cache` rather than `actions/cache`, matching `eqtylab/integrity`. The difference that
      # matters is what it saves: it prunes the target directory to dependency artifacts, dropping
      # `incremental/` and our own crates' output, so the cache holds the part that actually costs
      # the nine minutes and not the part that is rebuilt every run regardless.
      - name: Rust Cache
        uses: eqtylab-actions/rust-cache@v2
        with:
          # Two Cargo workspaces, both under `packages/`, each with its own Cargo.lock and target
          # directory. The default is a workspace at the repository root, which is neither of them.
          workspaces: |
            packages/eqty-lineage-nemo-relay -> target
            packages/eqty-lineage-nemo-relay/abi-test -> target
          # `add-job-id-key` keys on the job id, which both legs of a matrix share -- without this
          # they would compute one key and race to write it.
          key: ${{ matrix.suite }}
          env-vars: NIX_RUSTC_VERSION
          # Without this the cache is saved only when the whole job succeeds, so a pull request with
          # failing clippy recompiles the world on every push -- the loop where a cache is worth
          # most. A partial target directory is safe to restore; cargo fingerprints what it finds.
          cache-on-failure: true
      - name: Disk after restore
        shell: bash -e {0}
        run: df -h / | tail -1 | xargs echo "free after dev shell + restore:"
      # Needs no dependency tree, so running it in both legs costs nothing worth splitting.
      - name: Format
        run: just fmt-check-rust
      # Before the tests, so the dependency tree clippy compiles is the one `cargo test` reuses.
      - name: Lint
        run: just lint-rust-${{ matrix.suite }}
      - name: Test
        run: just test-rust-${{ matrix.suite }}
      # `always()`, because the reading matters most on the run that failed for want of space.
      - name: Disk after build
        if: always()
        shell: bash -e {0}
        run: df -h / | tail -1 | xargs echo "free after build:"

  # One place to read the outcome. Parallel jobs already fail the run on their own -- this exists so
  # the run summary names which one failed without opening each job. `if: always()` is what makes it
  # report a failure rather than being skipped by one.
  summary:
    runs-on: ubuntu-latest
    needs: [python, rust]
    if: always()
    defaults:
      run:
        shell: bash -e {0}
    steps:
      - name: Summarize checks
        run: |
          {
            echo "## CI results"
            echo
            echo "| Check | Result |"
            echo "| --- | --- |"
            echo "| Python | ${{ needs.python.result == 'success' && '🟢 Passed' || '🔴 Failed' }} |"
            echo "| Rust | ${{ needs.rust.result == 'success' && '🟢 Passed' || '🔴 Failed' }} |"
          } >> "$GITHUB_STEP_SUMMARY"
      - name: Fail if a check failed
        if: needs.python.result != 'success' || needs.rust.result != 'success'
        run: exit 1
